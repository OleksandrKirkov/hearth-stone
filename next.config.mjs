/** @type {import('next').NextConfig} */
const nextConfig = {
    //output: 'export',
    //basePath: '/health_stone',
    //assetPrefix: '/health_stone/',
    images: {
        unoptimized: false
    }
};

export default nextConfig;
